$extract{?} = extract
$mirror{?} = mirror

steps = extract mirror

extract.ns = xios
extract.location{primary}[xios]=${XIOS_REPO_URL}
extract.location[xios] =${XIOS_REPO_URL}@${XIOS_REV}
extract.location{diff}[xios]=${XIOS_SOURCES}
